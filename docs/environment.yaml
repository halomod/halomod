channels:
  - defaults
  - conda-forge

dependencies:
  - pip
  - ipython
  - nbsphinx
  - numpydoc
  - sphinx>=3.2
  - numpy
  - scipy
  - astropy  # need this here, because we base classes off it (and docs need that).
  - pyyaml
  - camb<=1.1.3
  - pip:
    - sphinx-rtd-theme
